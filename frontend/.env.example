# URL do backend. A porta deve casar com a porta PUBLICADA no host pelo
# docker-compose (BACKEND_PORT no .env raiz) — hoje 3001 neste ambiente porque
# 3000 pode estar ocupada. Se você mudar a porta publicada, mude aqui também.
VITE_API_URL=http://localhost:3001/api/v1
